	   // Prompt the user for their name, creates a string
	   const name = prompt('What is you name?');
	   
	   //checks how many characters the string has
	   const length = name.length;
	   if (length > 4) {

  // If the name langth is greater than 4, alert 'your name is greater than 4 characters'
    alert('Your name is greater than 4 characters');
	}
	
	else {
  // Otherwise, alert 'Your name is not grater than 4 characters'
    alert('Your name is not greater than 4 characters');
	}
